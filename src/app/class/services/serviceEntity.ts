import { API_CONFIG } from "src/app/config/api_config"
export abstract class ServiceEntity<E>{

   /*
     protected urlPathConfig(){
          return API_CONFIG.apiUrl + this.getPathUrl();
     }

     protected abstract getPathUrl():any;*/

}